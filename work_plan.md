# תוכנית עבודה — יישום צינור הערכת מנת אוכל

> מסמך תכנון יישום מפורט המגדיר את שלבי הפיתוח, התלויות, האבני דרך, וחלוקת האחריויות לבניית הצינור החדש
> **גרסה:** 1.0
> **מסמך מקושר:** [תוכנית הצינור](food_estimation_pipeline.md)

---

## תוכן עניינים

1. [סקירה כללית](#סקירה-כללית)
2. [החלטות ארכיטקטוניות מקדימות](#החלטות-ארכיטקטוניות-מקדימות)
3. [מבנה המודולים החדש](#מבנה-המודולים-החדש)
4. [שלבי הפיתוח](#שלבי-הפיתוח)
   - [אבן דרך 1 — תשתית והכנה](#אבן-דרך-1--תשתית-והכנה)
   - [אבן דרך 2 — קליברציה ומדידת כלי](#אבן-דרך-2--קליברציה-ומדידת-כלי)
   - [אבן דרך 3 — זיהוי ומדידת מאכלים](#אבן-דרך-3--זיהוי-ומדידת-מאכלים)
   - [אבן דרך 4 — חישוב נפח ומשקל](#אבן-דרך-4--חישוב-נפח-ומשקל)
   - [אבן דרך 5 — צפיפות ותזונה](#אבן-דרך-5--צפיפות-ותזונה)
   - [אבן דרך 6 — סקירה ואריזה](#אבן-דרך-6--סקירה-ואריזה)
   - [אבן דרך 7 — אינטגרציה והתאמת לקוח](#אבן-דרך-7--אינטגרציה-והתאמת-לקוח)
   - [אבן דרך 8 — בדיקות וניקיון](#אבן-דרך-8--בדיקות-וניקיון)
5. [חלוקת אחריויות](#חלוקת-אחריויות)
6. [תלויות בין משימות](#תלויות-בין-משימות)
7. [אסטרטגיית בדיקות](#אסטרטגיית-בדיקות)
8. [ניהול סיכונים וחלופות](#ניהול-סיכונים-וחלופות)
9. [מה נשאר ומה נמחק מהקוד הקיים](#מה-נשאר-ומה-נמחק-מהקוד-הקיים)

---

## סקירה כללית

### מטרת התוכנית

הצינור החדש דורש שכתוב מהותי של רוב שירותי הסריקה. תוכנית העבודה מחלקת את העבודה לשמונה אבני דרך עוקבות, כל אחת עם תוצרי בדיקה ברורים שמאפשרים בדיקה ודיבוג עצמאית לפני המעבר הלאה.

### עקרונות הפיתוח

- **בנייה מלמטה למעלה** — שלבים שלא תלויים בשרשור AI נבנים ראשונים (מתמטיקה, פילוח, USDA)
- **כל שלב הוא class עצמאי** — אפשר לבדוק ולהחליף ספק ללא נגיעה בשאר הצינור
- **בדיקות לפני אינטגרציה** — כל מודול חדש נבדק עצמאית לפני שהוא משתלב
- **המסלול הקיים לא נפגע** עד אבן דרך 7 — מאפשר פיתוח במקביל לשימוש ייצור
- **גישה מצטברת** — אפשר לעצור בכל אבן דרך ולקבל ערך גם אם לא ממשיכים

### מסגרת זמן משוערת

זה ייאמד אחרי קבלת החלטות מקדימות. כרגע מבנה המסמך נטול הערכות זמן מוחלטות, אך מסודר בסדר ביצוע אופטימלי.

---

## החלטות ארכיטקטוניות מקדימות

לפני התחלת הקוד, יש לקבל את ההחלטות הבאות. כל החלטה מטה משפיעה על מספר אבני דרך.

### החלטה ראשונה — מודל הראייה

**הקיים:** Gemini Pro למשימות vision, Gemini Flash למשימות טקסט.

**הצינור דורש:** Pro לשלבים שניים, שלישי, וחמישי. Flash לשלב עשירי.

**צריך לאשר:** האם נמשיך עם Gemini או נשקול ספק נוסף לאמינות מדידות?

**המלצה:** להישאר עם Gemini. הצינור החדש מפצל את המשימות לקריאות ממוקדות, מה שמתקן את הבעיה העיקרית של דיוק. ניסויים יתחילו עם Pro לכל שלבי המדידה.

### החלטה שנייה — אובייקט הייחוס

**שאלה:** האם להגביל לסוג אחד של אובייקט ייחוס או לתמוך במספר?

**המלצה:** להתחיל עם סוג אחד דומיננטי (כרטיס אשראי או מטבע סטנדרטי) ולהוסיף תמיכה בנוספים אחרי שהצינור יציב. הוספה דורשת רק עדכון של מאגר הממדים הפיזיים, לא שינוי לוגי.

### החלטה שלישית — מקור בסיס נתונים תזונתי

**הקיים:** USDA FoodData Central.

**צריך לאשר:** האם USDA מספיק או נדרש מקור נוסף לפיצוי על מאכלים שאינם אמריקאים (פלאפל, חומוס, שווארמה)?

**המלצה:** להישאר עם USDA לבסיס, ולהוסיף מנגנון fallback למאכלים מקומיים בעתיד. במאכלים חסרים — בקשת קלט ידני מהמשתמש.

### החלטה רביעית — סדרי גודל ויחידות

**עיקרון מנחה:** כל מדידה בשרת בסנטימטרים, גרמים, או ערכים יחסיים. אין יחידות אמריקאיות. כל המרה לתצוגה למשתמש קורית בלקוח לפי העדפת המשתמש.

### החלטה חמישית — אסטרטגיית קליברציה כפולה

**שאלה:** מה עושים כשהפער בין שני יחסי הקליברציה גדול מהסף?

**אפשרויות:**

1. **דחיית הצינור** — מבקשים מהמשתמש לצלם מחדש
2. **שימוש ביחס הבטוח יותר** — בוחרים את היחס עם ה-confidence הגבוה יותר
3. **ממוצע משוקלל** — לפי confidence לכל יחס

**המלצה:** אופציה ראשונה. במצב של פער גדול, סביר שאחת התמונות באיכות נמוכה ולא נכון להמשיך.

### החלטה שישית — נקודת הקפיצה למסלול הישן

**שאלה:** האם בכישלון של אחד משלבי הצינור החדש, נוסיף fallback למסלול הישן?

**המלצה:** לא בשלב הראשון. הצינור החדש חייב לעמוד בעצמו או לדחות את הבקשה. הוספת fallback מורכבת ומוסיפה מסלולי בדיקה. אחרי בדיקות שטח, תוסיף בעתיד אם יש צורך.

---

## מבנה המודולים החדש

### תרשים שכבות

```
┌─────────────────────────────────────────────────┐
│              ScanController                      │
│  (נקודת כניסה, תזמור הצינור)                    │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│         FoodEstimationPipeline                   │
│  (תזמור שלב אחר שלב, ניהול state)               │
└──┬───┬───┬───┬───┬───┬───┬───┬───┬───┬──────────┘
   │   │   │   │   │   │   │   │   │   │
   ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼
  S1  S2  S3  S4  S5  S6  S7  S8  S9  S10
```

### Class-ים חדשים

#### שכבת תזמור

| Class | אחריות | החלפה ל |
| --- | --- | --- |
| `FoodEstimationPipeline` | תזמור כל אחד עשר השלבים, ניהול failure points | תפקיד דומה ל-`ScanServiceImpl` הישן |
| `PipelineContext` | מבנה נתונים שעובר בין שלבים, מצטבר תוצרים | חדש — לא קיים בקוד היום |
| `PipelineResult` | תוצאה סופית עם כל המידע ואזהרות | משופר — מבוסס על `MealBoundary` הישן |

#### שכבת השלבים

| Class | אחריות | טכנולוגיה |
| --- | --- | --- |
| `ImageQualityValidator` (לקוח) | בדיקות איכות תמונה לפני שליחה | בדיקות מקומיות |
| `CalibrationService` | שלב 1 — מציאת אובייקט ייחוס וחישוב יחס | OpenCV |
| `PlateGeometryService` | שלב 2 — מדידת ממדי הכלי | Gemini Pro |
| `FoodDetectionService` | שלב 3 — זיהוי מאכלים ומיקום | Gemini Pro |
| `FoodAreaService` | שלב 4 — חידוד שטח באמצעות פילוח | OpenCV GrabCut |
| `FoodHeightService` | שלב 5 — מדידת גובה ערימה | Gemini Pro |
| `VolumeCalculator` | שלב 6 — חישוב נפח | מתמטיקה |
| `NutritionAndDensityService` | שלב 7 — שאילתות USDA וחישוב צפיפות | USDA + מתמטיקה |
| `WeightCalculator` | שלב 8 — חישוב משקל | מתמטיקה |
| `NutritionAggregator` | שלב 9 — צבירה לרמת ארוחה | מתמטיקה |
| `SanityCheckService` | שלב 10 — סקירת מודל קל | Gemini Flash |

#### שכבת תמיכה

| Class | אחריות |
| --- | --- |
| `ReferenceObjectRegistry` | מאגר ממדים פיזיים של אובייקטי ייחוס נתמכים |
| `DensityCalculator` | אסטרטגיות לחישוב צפיפות מנתוני USDA |
| `CrossValidationService` | אוסף בדיקות הצלבה משותפות לכל הצינור |
| `ConfidenceAggregator` | חישוב רמת ביטחון משוקללת |
| `PipelineWarningCollector` | איסוף וניהול אזהרות מכל השלבים |

### מבני נתונים

#### `PipelineContext` (מצטבר)

```
- imageA, imageB
- referenceObjectType
- pixelToCmRatioA, pixelToCmRatioB
- referenceBoundsA, referenceBoundsB
- plateBoundsA
- plateGeometry (diameter, depth, type, fillPercent)
- foodItems (רשימה, כל פריט מצטבר עם הזמן)
- warnings
- timings (לכל שלב)
- confidences (לכל שלב)
```

#### `FoodItem` (מצטבר לאורך השלבים)

```
שלב 3 ממלא:
- name
- baseIngredient
- visualState
- boundingBox
- searchTerms
- detectionConfidence
- requiresUserValidation

שלב 4 ממלא:
- areaCm2
- coveragePercent
- areaConfidence

שלב 5 ממלא:
- effectiveHeightCm
- heightConfidence
- heightWarnings

שלב 6 ממלא:
- volumeCm3

שלב 7 ממלא:
- nutritionPer100g (carbs, protein, fat, fiber, water)
- densityGPerCm3
- densitySource
- foundInUsda
- usdaItemId

שלב 8 ממלא:
- weightG
- weightWarnings

שלב 9 ממלא:
- carbsG, netCarbsG, proteinG, fatG, fiberG
```

---

## שלבי הפיתוח

### אבן דרך 1 — תשתית והכנה

**מטרה:** להניח את היסודות לפני כל פיתוח שלבים.

#### משימות

##### 1.1 — הגדרת מבני נתונים בסיסיים

צור את ה-class-ים הבאים בשרת:

- `PipelineContext` — מבנה נתונים שיועבר בין שלבים
- `FoodItem` — pojo עם כל השדות שיתמלאו לאורך הצינור
- `PipelineResult` — מבנה התוצאה הסופית
- `PipelineWarning` — מבנה אזהרה אחיד עם type, severity, sourceStage, message
- `StageResult<T>` — wrapper גנרי שמחזיר תוצאה או failure עם reason

##### 1.2 — שכבת ה-pipeline בסיסית

צור את `FoodEstimationPipeline` בלי תוכן בשלבים. זה רק skeleton:

- מקבל את הקלט של הלקוח
- מאתחל `PipelineContext`
- מריץ שלב אחרי שלב באופן רציף
- מטפל ב-failure של שלב (עוצר את הצינור או ממשיך עם confidence נמוכה)
- מחזיר `PipelineResult`

##### 1.3 — שירותי תמיכה כלליים

צור:

- `CrossValidationService` — בדיקות הצלבה משותפות
- `ConfidenceAggregator` — חישוב confidence משוקלל
- `PipelineWarningCollector` — איסוף אזהרות
- `ReferenceObjectRegistry` — מאגר ממדים פיזיים

##### 1.4 — endpoint חדש בקונטרולר

צור endpoint חדש בנפרד מהקיים, למשל `/api/v2/scan`. שני endpoints יחיו במקביל עד אבן דרך 7.

##### 1.5 — בדיקת רמות לוג ומטריקות

ודא שכל שלב יכול לרשום:

- כניסה לשלב עם input summary
- יציאה מהשלב עם output summary
- זמן ביצוע
- שגיאות

#### קריטריון סיום

- כל ה-class-ים קיימים ועוברים compile
- אפשר לקרוא ל-endpoint החדש עם תמונות placeholder ולקבל `PipelineResult` ריק

---

### אבן דרך 2 — קליברציה ומדידת כלי

**מטרה:** לסיים את שני השלבים הראשונים, שכוללים את כל המידע על הכלי לפני שניגשים למאכלים.

#### משימות

##### 2.1 — בניית `CalibrationService` (שלב 1)

מודול חדש שמקבל תמונה ו-`referenceObjectType`, מחזיר:

- `pixelToCmRatio`
- `referenceBounds`

המודול:

- משתמש ב-OpenCV (כבר קיים ב-codebase)
- שואב את הממדים הפיזיים מ-`ReferenceObjectRegistry`
- מחזיר `StageResult.Failure` אם לא נמצא

הקוד נכתב פעם אחת ונקרא פעמיים בצינור — פעם לתמונה העליונה ופעם לצדדית.

##### 2.2 — בדיקות יחידה ל-`CalibrationService`

הכן סט תמונות בדיקה:

- תמונה עם אובייקט ברור ולא מוסתר
- תמונה עם אובייקט בקצה
- תמונה ללא אובייקט (אמורה להחזיר failure)
- תמונה עם אובייקט מוסתר חלקית (אמורה להחזיר confidence נמוך)

##### 2.3 — Cross-validation בין שני יחסי הקליברציה

ב-`FoodEstimationPipeline`, אחרי שני קריאות ל-`CalibrationService`:

- חישוב הפער היחסי
- אם מעל סף — אזהרה והפסקת זרם (לפי החלטה חמישית)

##### 2.4 — בניית `PlateGeometryService` (שלב 2)

מודול חדש שמקבל את שתי התמונות ושני יחסי הקליברציה, מחזיר:

- `plateDiameterCm`
- `plateDepthCm`
- `containerType`
- `containerFillPercent`
- confidence נפרדים

המודול:

- בונה prompt ל-Gemini Pro לפי הספציפיקציה בשלב 2 של תוכנית הצינור
- שולח את שתי התמונות בקריאה אחת
- מנתח את התשובה
- מבצע את הצלבה הפנימית (סוג כלי לעומת עומק)

##### 2.5 — בדיקות יחידה ל-`PlateGeometryService`

הכן סט תמונות:

- צלחת שטוחה ברורה
- קערה עם תוכן
- קערה עמוקה
- כלי בלתי-סטנדרטי (אמור להחזיר confidence נמוך)

#### קריטריון סיום

- שני הסניות עוברים endpoint-ית
- אפשר לקבל מהצינור: שני יחסי קליברציה + ארבעה ערכי גיאומטריה + confidence-ים
- אזהרות נכונות מופיעות במקרי קצה

---

### אבן דרך 3 — זיהוי ומדידת מאכלים

**מטרה:** לסיים את שלבים שלוש, ארבע, וחמש — שהם הליבה של תיאור המנה.

#### משימות

##### 3.1 — בניית `FoodDetectionService` (שלב 3)

מודול חדש שמקבל תמונה עליונה, מחזיר רשימה של `FoodItem` עם השדות של שלב 3.

המודול:

- בונה prompt ל-Gemini Pro לפי הספציפיקציה
- מקבל JSON עם רשימת מאכלים
- ממפה ל-`FoodItem`
- כל פריט מקבל confidence עצמאי

##### 3.2 — בדיקות יחידה ל-`FoodDetectionService`

הכן סט תמונות:

- צלחת עם מאכל אחד
- צלחת עם 3-4 מאכלים נפרדים
- מאכל מורכב (סלט) — לבדוק האם המודל מפרק לרכיבים או מחזיר כסלט
- צלחת עם מאכל ואובייקט ייחוס (לוודא שהאובייקט לא נכלל)

##### 3.3 — בניית `FoodAreaService` (שלב 4)

מודול חדש שמקבל תמונה עליונה ורשימת bounding boxes, ומריץ:

1. זיהוי גבולות הצלחת ב-OpenCV (Hough Circles) — במקביל לפילוח
2. עבור כל פריט: GrabCut על אזור ה-bbox
3. חיתוך לגבולות הצלחת
4. הסרת אובייקט הייחוס
5. ספירת פיקסלים והמרה ל-cm²

המודול דורש זמן עבודה משמעותי כי הוא מחליף קוד קיים שהיה בלקוח.

##### 3.4 — מעבר מלקוח לשרת של GrabCut

הקוד של GrabCut קיים היום ב-`PortionEstimator.kt` באנדרואיד. צריך:

- להעביר את הלוגיקה לשרת (Java OpenCV)
- לבדוק שהפלטים זהים על אותם תשומות
- לוודא שתלות OpenCV בשרת תקינה

##### 3.5 — בדיקות יחידה ל-`FoodAreaService`

הכן סט תמונות + bounding boxes ידועים:

- מאכל יחיד עם רקע ברור
- מאכל קרוב לקצה הצלחת
- שני מאכלים סמוכים (לבדוק שלא חופפים)
- מאכל שתיבת ההיקף שלו חופפת לאובייקט הייחוס

לכל מקרה: לבדוק את השטח שמוחזר מול ערך אמיתי שנמדד ידנית.

##### 3.6 — בניית `FoodHeightService` (שלב 5)

מודול חדש שמקבל תמונה צדדית, יחס פיקסל לסנטימטר, ורשימת שמות מאכלים.

המודול:

- בונה prompt ל-Gemini Pro עם ההגדרה המדויקת של "גובה אפקטיבי"
- שולח את התמונה
- מפענח את התשובה
- מקבל confidence ואזהרות לכל מאכל

##### 3.7 — Cross-validation בין גובה לעומק

ב-`FoodEstimationPipeline`:

- לכל מאכל, אם גובה > עומק כלי, סימון אזהרה
- חריג: גלישה החוצה (כמו המבורגר על צלחת שטוחה) זה לא בעיה

##### 3.8 — בדיקות יחידה ל-`FoodHeightService`

הכן סט תמונות צדדיות:

- מאכל פלאט (פרוסת לחם)
- ערימה גבוהה (מנת אורז עם פסגה)
- מאכל אווריר (סלט עלים)
- מאכל מוסתר חלקית (אמור להחזיר warning)

#### קריטריון סיום

- אחרי שלב 5, יש בידי הצינור: שטח מדויק + גובה אפקטיבי לכל מאכל
- כל הבדיקות עוברות

---

### אבן דרך 4 — חישוב נפח ומשקל

**מטרה:** לבנות את שכבת המתמטיקה הטהורה — שלבים שש ושמונה. שלב 7 ידלג זמנית עם mock כדי לאפשר בדיקה.

#### משימות

##### 4.1 — בניית `VolumeCalculator` (שלב 6)

class פשוט עם מתודה אחת: `calculate(area, height) -> volume`. שום AI, שום DB.

הוסף הצלבה: סכום נפחים מול נפח כלי.

##### 4.2 — בדיקות יחידה ל-`VolumeCalculator`

טסטים פשוטים — קלט מחושב, פלט מצופה.

##### 4.3 — בניית `WeightCalculator` (שלב 8)

class פשוט עם מתודה: `calculate(volume, density) -> weight`.

הוסף את גבולות ההיגיון (משקל בין שלושה לחמש מאות גרם).

##### 4.4 — בדיקות יחידה ל-`WeightCalculator`

טסטים מתמטיים פשוטים.

##### 4.5 — Mock זמני ל-`NutritionAndDensityService`

עד אבן דרך 5, צור מימוש mock שמחזיר density מקובע (למשל 0.8 לכל מאכל) כדי לאפשר בדיקה של 6 ו-8 בצינור.

#### קריטריון סיום

- אחרי שלב 8, יש בידי הצינור משקל לכל מאכל (גם אם הצפיפות mock)
- בדיקות עוברות

---

### אבן דרך 5 — צפיפות ותזונה

**מטרה:** לבנות את השלב הקריטי ביותר — שלב 7 — עם כל אסטרטגיות הצפיפות.

#### משימות

##### 5.1 — שיפור integration ל-USDA

הקוד הקיים ל-USDA חי ב-`UsdaService`. צריך:

- לוודא שהוא מחזיר את כל ה-foodPortions (לשיטה ראשונה של צפיפות)
- לוודא שהוא מחזיר את כל ה-proximates (לשיטה שנייה)
- להוסיף בחירת מועמד טוב יותר לפי מצב חזותי

##### 5.2 — בניית `DensityCalculator`

מודול חדש עם שלוש אסטרטגיות:

- **שיטה ראשונה** — חישוב מ-foodPortion עם volume
- **שיטה שנייה** — חישוב מ-proximates לפי נוסחת Choi-Okos
- **שיטה שלישית** — שיטה שנייה עם מקדם אוויר ממודל

המודול בודק שיטה ראשונה, ואם לא ניתן, עובר לשנייה. אם המאכל סומן כאווריר, מפעיל גם שיטה שלישית.

##### 5.3 — הוספת מקדם אווירי לשלב 3

נדרש שינוי ב-`FoodDetectionService`:

- ה-prompt מבקש בנוסף `effectiveDensityFactor` לכל מאכל אם הוא אווריר
- הערך מועבר הלאה ב-`FoodItem`

##### 5.4 — בניית `NutritionAndDensityService` (שלב 7)

מודול חדש שמקבל רשימת `FoodItem`, ולכל אחד:

- מבצע חיפוש USDA לפי search terms
- בוחר מועמד מתאים
- מפעיל `DensityCalculator` עם השיטה המתאימה
- ממלא ב-`FoodItem` את כל השדות התזונתיים

##### 5.5 — מקרה קצה: מאכל לא נמצא ב-USDA

הוסף לוגיקה:

- סימון `foundInUsda = false`
- `densitySource = UNKNOWN`
- weightG ב-null עם reason ברור
- הוספה לרשימת `itemsRequiringValidation`

##### 5.6 — החלפת ה-mock במימוש האמיתי

ב-`FoodEstimationPipeline`, החלף את ה-mock של אבן דרך 4 במימוש האמיתי.

##### 5.7 — בדיקות יחידה ל-`NutritionAndDensityService`

הכן בדיקות:

- מאכל ידוע ופשוט (אורז) — לבדוק שלוש השיטות
- מאכל אווריר (לחם) — לבדוק שיטה שלישית
- מאכל לא קיים — לבדוק החזרה מתאימה
- מאכל עם 2+ search terms — לבדוק שעובר על כולם

#### קריטריון סיום

- אחרי אבן דרך 5, הצינור הסרברי שלם
- כל הערכים התזונתיים מגיעים מ-USDA
- אפשר לקרוא ל-endpoint עם תמונות אמיתיות ולקבל תוצאה תזונתית מלאה

---

### אבן דרך 6 — סקירה ואריזה

**מטרה:** להוסיף את הסקירה האחרונה ולוודא שהפלט נארז כהלכה.

#### משימות

##### 6.1 — בניית `NutritionAggregator` (שלב 9)

class מתמטי טהור שמקבל את כל ה-`FoodItem` ומפיק סיכומים ברמת ארוחה.

##### 6.2 — בניית `SanityCheckService` (שלב 10)

מודול חדש שמקבל תמונה עליונה ותוצאה מלאה, ומחזיר רשימת אזהרות.

המודול:

- בונה prompt ל-Gemini Flash עם תיאור התוצאה
- מקבל ניתוח טקסטואלי
- ממפה לרשימת אזהרות מובנות

##### 6.3 — בנייה סופית של `PipelineResult`

המבנה המלא לפי שלב 11 בתוכנית הצינור:

- מאכל בודד עם כל השדות
- רמת ארוחה עם סיכומים
- מטא-נתונים עם אזהרות, timings, וכו'

##### 6.4 — חישוב confidence כולל

`ConfidenceAggregator` משקלל את כל ה-confidences לאורך הצינור. הציון הסופי מסומן בולט בתוצאה.

##### 6.5 — בדיקות end-to-end

הרץ את הצינור על 10-15 ארוחות שונות:

- מנה פשוטה (אורז + עוף + ירק)
- מנה מורכבת (פסטה עם רוטב)
- מנה אווירית (סלט)
- מנת קערה (מרק)
- מנה לא מזוהה היטב

עבור כל מקרה — תיעוד מספרים, השוואה למשקל אמיתי במאזניים.

#### קריטריון סיום

- end-to-end עובר על מנות אמיתיות
- דיוק משוער של ±20% במשקל האמיתי

---

### אבן דרך 7 — אינטגרציה והתאמת לקוח

**מטרה:** לחבר את הצינור החדש ללקוח ולהחליף את המסלול הישן.

#### משימות

##### 7.1 — שינויים בלקוח לאיסוף שתי תמונות

ב-Android, שינויים ב-`CameraScanFragment`:

- זרימת UX לצילום ראשון (top-down)
- מסך מעבר עם הסבר
- זרימת UX לצילום שני (side)
- אימות איכות לפני שליחה

זוהי משימה משמעותית של UX — דורשת אישור עיצוב מ-Daniel.

##### 7.2 — `ImageQualityValidator` בלקוח

class חדש שבודק לפני שליחה:

- bright/sharp/resolution
- האם יש hint של אובייקט ייחוס

##### 7.3 — שינוי endpoint ב-Retrofit

עדכון של ה-API client לקרוא ל-endpoint החדש עם שתי תמונות.

##### 7.4 — שינויים ב-UI של תוצאות

ב-Android, מסך התוצאות:

- הצגת אזהרות בולטת
- אפשרות לעריכה ידנית של פריטים שדורשים סקירה
- הצגת confidence per-item ולא רק כוללת

##### 7.5 — מחיקת קוד מיותר בלקוח

מחק/הוצא משימוש:

- קוד הערכת volume בלקוח
- קוד GrabCut בלקוח (העבר לשרת)
- כל לוגיקה של scan שאינה תפקיד הלקוח

##### 7.6 — מחיקת קוד מיותר בשרת

מחק/הוצא משימוש את ה-endpoints וה-services הישנים אחרי שהחדשים מוכחים בשטח.

##### 7.7 — בדיקות אינטגרציה מלאות עם המכשיר

הרץ את הצינור הסופי על 10+ ארוחות אמיתיות, עם תמונות אמיתיות מהמכשיר.

#### קריטריון סיום

- הלקוח שולח שתי תמונות, מקבל תוצאה תזונתית
- ה-UI מציג את כל המידע כראוי
- המסלול הישן נמחק

---

### אבן דרך 8 — בדיקות וניקיון

**מטרה:** לוודא שהמערכת יציבה ומוכנה לבדיקות חיצוניות עם Tom.

#### משימות

##### 8.1 — בדיקות שטח עם משתמשים אמיתיים

מבחן עם 5-10 משתמשים:

- ארוחות אמיתיות במהלך היום
- מילוי שאלון על דיוק תפישתי
- השוואה למאזניים

##### 8.2 — תיעוד פנימי

עדכון של:

- מסמך ארכיטקטורה (תוכנית הצינור)
- README של ה-API
- תיעוד של כל service חדש

##### 8.3 — תיעוד עבור Tom

הכנת:

- תיעוד שימוש
- תיאור התרחישים שכדאי לבדוק
- מה מצופה מהבודק לדווח

##### 8.4 — מטריקות ביצועים

הוספת ניטור:

- זמן ביצוע ממוצע לצינור (יעד: מתחת ל-12 שניות)
- אחוז כשלון בכל שלב
- שכיחות אזהרות

##### 8.5 — אופטימיזציות אם נדרש

אם ביצועים תחת היעד:

- שלבי AI במקביל איפה שאפשר (שלב 2 ו-3 לא תלויים)
- caching של USDA queries
- ניתוח של זמני שלבים

##### 8.6 — תיעוד החלטות אדריכליות

קובץ ADR (Architecture Decision Record) לכל החלטה משמעותית שהתקבלה במהלך הפיתוח.

#### קריטריון סיום

- מערכת יציבה
- בדיקות חיצוניות התחילו
- תיעוד מלא

---

## חלוקת אחריויות

### נמרוד (שרת + ארכיטקטורה)

| אבן דרך | משימות עיקריות |
| --- | --- |
| 1 | כל המשימות של אבן הדרך |
| 2 | כל המשימות (CalibrationService, PlateGeometryService) |
| 3 | FoodDetectionService, FoodAreaService, FoodHeightService, ההעברה של GrabCut |
| 4 | כל המשימות (חישובים מתמטיים) |
| 5 | כל המשימות (NutritionAndDensityService, DensityCalculator) |
| 6 | NutritionAggregator, SanityCheckService, PipelineResult |
| 7 | שינויי endpoint, מחיקת קוד שרת ישן |
| 8 | מטריקות ביצועים, תיעוד, אופטימיזציות |

### דניאל (לקוח + UX)

| אבן דרך | משימות עיקריות |
| --- | --- |
| 7 | זרימת UX לצילום שתי תמונות, ImageQualityValidator, UI של תוצאות, מחיקת GrabCut מהלקוח |
| 8 | מבחני שטח עם המכשיר |

### משותף

| משימה | פירוט |
| --- | --- |
| החלטות מקדימות | יושבים יחד לפני אבן דרך 1 |
| בדיקות end-to-end (אבן דרך 6) | נמרוד מריץ ב-API, דניאל בודק בלקוח |
| בדיקות אינטגרציה (אבן דרך 7) | ביחד על המכשיר |
| בדיקות שטח (אבן דרך 8) | ביחד עם המשתמשים |

### Tom Cohen (מנחה)

מעורב בנקודות החלטה משמעותיות:

- אישור החלטות מקדימות
- ביקורת על ארכיטקטורת הצינור
- אישור לפני בדיקות שטח עם Tom החיצוני

### Tom (בודק חיצוני)

נכנס בסוף אבן דרך 8 — בדיקות שטח רחבות.

---

## תלויות בין משימות

### תלות חמורה (Hard Dependencies)

```
אבן דרך 1 (תשתית)
      │
      ▼
אבן דרך 2 (קליברציה + כלי)
      │
      ▼
אבן דרך 3 (מאכלים)
      │
      ▼
אבן דרך 4 (נפח + משקל מ-mock)
      │
      ▼
אבן דרך 5 (תזונה + צפיפות אמיתית)
      │
      ▼
אבן דרך 6 (אריזה + סקירה)
      │
      ▼
אבן דרך 7 (אינטגרציית לקוח)
      │
      ▼
אבן דרך 8 (בדיקות שטח)
```

### תלות חלשה (יכולה להיעשות במקביל)

- **אבן דרך 7.1** (UX לצילום) — אפשר להתחיל עוד באבן דרך 2, אחרי החלטות מקדימות
- **אבן דרך 8.3** (תיעוד עבור Tom) — אפשר לכתוב במקביל לאבן דרך 7
- **משימות בדיקות יחידה** בכל אבן דרך — אפשר לכתוב במקביל למימוש

### צוואר בקבוק

- אבן דרך 3.4 — העברת GrabCut מלקוח לשרת. תלוי באישור OpenCV בשרת
- אבן דרך 5.1 — שיפור integration ל-USDA. תלוי בהבנה מלאה של schema של USDA

---

## אסטרטגיית בדיקות

### שכבות בדיקה

#### בדיקות יחידה (Unit Tests)

לכל service חדש. לפחות:

- מקרה הצלחה רגיל
- מקרה כשלון רגיל (קלט שגוי)
- מקרה קצה (ערכים גבולות)
- מקרה של failure (לא נמצא, לא הגיוני)

#### בדיקות אינטגרציה (Integration Tests)

לכל אבן דרך:

- הצינור (חלקי) מקצה לקצה
- בדיקה שכל ה-cross-validation פועלים
- בדיקה שאזהרות מופיעות במקרים הנכונים

#### בדיקות end-to-end

מריצים את כל הצינור על תמונות אמיתיות, משווים לערכים אמיתיים.

#### בדיקות ביצועים

- זמן ביצוע ממוצע
- זמן ביצוע מקסימלי
- שכיחות תקלות

### מאגר תמונות בדיקה

יש להכין לפני אבן דרך 2:

- 30+ ארוחות אמיתיות שצולמו בזוגות (top + side)
- כל אחת עם משקל אמיתי במאזניים
- מגוון: מנות בודדות, מנות מורכבות, אווריריות, נוזליות
- לכל מנה: רישום של כל המאכלים והמשקלים בנפרד

### מטריקת הצלחה

- **דיוק משקל כולל:** ±15% מהמשקל האמיתי
- **דיוק פחמימות:** ±20% מהערך האמיתי (מה שמשפיע על מינון)
- **שיעור זיהוי מאכלים:** מעל 90% של מאכלי בסיס
- **זמן ביצוע ממוצע:** מתחת ל-12 שניות
- **שיעור אזהרות שגויות:** מתחת ל-15%

---

## ניהול סיכונים וחלופות

### סיכון 1 — Gemini לא מחזיר מדידות מספיק מדויקות

**ראייה:** במהלך אבן דרך 2 או 5, יוצא שהמדידות שמודל הראייה מספק (קוטר כלי, גובה ערימה) לא מספיק מדויקות.

**אופציות חלופיות:**

1. עיון מחדש ב-prompt עם דוגמאות קונקרטיות (few-shot learning)
2. מעבר לשני מודלים שונים והצלבה ביניהם
3. שילוב ARCore לעומק במקום בלעדיות על Gemini (החזרת חיישני עומק)

### סיכון 2 — GrabCut בשרת איטי או לא יציב

**ראייה:** באבן דרך 3.4, ההעברה לא עובדת חלק.

**אופציות חלופיות:**

1. הישארות עם GrabCut בלקוח (מסבך את הארכיטקטורה אבל עובד)
2. החלפה לאלגוריתם פילוח אחר (SAM, מודלים מבוססי deep learning)

### סיכון 3 — USDA חסר עבור מאכלים מקומיים

**ראייה:** משתמשים מצלמים מאכלים שלא קיימים ב-USDA (פלאפל, חומוס, שווארמה).

**אופציות חלופיות:**

1. הוספת מקור משלים (USDA + Israeli food database)
2. הזנה ידנית של תזונה למאכלים נפוצים
3. בקשה מהמשתמש להזין משקל וערכים תזונתיים

### סיכון 4 — תוצאות לא יציבות בין צילומים

**ראייה:** אותה ארוחה מצולמת פעמיים, מתקבלות תוצאות שונות מעל 30%.

**אופציות חלופיות:**

1. ניתוח הסיבה (איזה שלב מייצר את הסטייה)
2. הגברת הצלבות
3. אם מקור הבעיה ב-Gemini — ביצוע 2 קריאות ובחירת תוצאה אחת

### סיכון 5 — הצינור איטי מדי (מעל 15 שניות)

**ראייה:** באבן דרך 6 או 8, זמן ביצוע מעל היעד.

**אופציות חלופיות:**

1. שלבים במקביל (שלב 2 ו-3)
2. caching אגרסיבי של USDA
3. שימוש ב-Gemini Flash לחלק מהמדידות (פשרה על דיוק)

---

## מה נשאר ומה נמחק מהקוד הקיים

### נשאר ועובר עדכון

| קוד קיים | מה קורה |
| --- | --- |
| `UsdaService` | משופר באבן דרך 5.1 — מחזיר foodPortions ו-proximates |
| `GeminiApiClient` | נשאר כתשתית, ה-services החדשים משתמשים בו |
| OpenCV reference detection | משופר ועוטף ב-`CalibrationService` |
| `MealEntity` ו-`MealBoundary` | מורחבים עם השדות החדשים |
| `CameraScanFragment` | מותאם לזרימה החדשה (אבן דרך 7) |

### נמחק

| קוד קיים | סיבה |
| --- | --- |
| `ScanServiceImpl` (במהלך הזמן) | מוחלף ב-`FoodEstimationPipeline` |
| `getShapeFactor` | קבועים מהונדסים — אסור |
| `getBowlGeometryFactor` | קבועים מהונדסים — אסור |
| Volume estimation בלקוח | האחריות עוברת לשרת |
| GrabCut בלקוח (`PortionEstimator.kt`) | האחריות עוברת לשרת |
| Fallback logic ל-volume בשרת | במקום זה, החזרת null עם reason |

### מאופס/נכתב מחדש

- כל ה-prompts ל-Gemini — חדשים לחלוטין, ממוקדים ושונים מהקיים
- מבנה ה-response של ה-scan API — מבוסס על `PipelineResult` החדש
- לוגיקת ה-error handling — מבוססת על `StageResult<T>`

---

## נספח — סיכום אבני הדרך בטבלה אחת

| אבן דרך | תוצר עיקרי | קריטריון סיום |
| --- | --- | --- |
| 1 | תשתית | endpoint עובד עם תוצאה ריקה |
| 2 | קליברציה ומדידת כלי | ארבעה ערכי גיאומטריה תקינים |
| 3 | זיהוי ומדידת מאכלים | רשימת מאכלים עם שטח וגובה |
| 4 | נפח ומשקל (mock) | משקלים מתקבלים גם אם צפיפות mock |
| 5 | צפיפות אמיתית | תוצאה תזונתית מלאה |
| 6 | אריזה וסקירה | end-to-end בשרת מקצה לקצה |
| 7 | אינטגרציית לקוח | המסלול החדש עובד מהמכשיר |
| 8 | בדיקות שטח | מערכת יציבה לבדיקות חיצוניות |

---

> **הערה לסיום:** התוכנית הזו מסודרת בסדר ביצוע אופטימלי, אך יכולה להתעדכן בהתאם לממצאים בשטח. כל אבן דרך עומדת בעצמה ומספקת ערך גם אם הפיתוח נעצר זמנית.
